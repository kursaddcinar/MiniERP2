@model MiniERP.Web.Models.LoginDto
@{
    Layout = "_Layout";
    ViewData["Title"] = "Giriş Yap";
}

<div class="container-fluid vh-100 d-flex align-items-center justify-content-center bg-light">
    <div class="card shadow-lg" style="max-width: 400px; width: 100%;">
        <div class="card-body p-5">
            <div class="text-center mb-4">
                <i class="fas fa-chart-line fa-3x text-primary mb-3"></i>
                <h3 class="card-title">MiniERP</h3>
                <p class="text-muted">Lütfen giriş yapın</p>
            </div>

            <form asp-controller="Auth" asp-action="Login" method="post">
                <div asp-validation-summary="All" class="text-danger mb-3"></div>

                <div class="mb-3">
                    <label asp-for="Username" class="form-label">Kullanıcı Adı</label>
                    <div class="input-group">
                        <span class="input-group-text">
                            <i class="fas fa-user"></i>
                        </span>
                        <input asp-for="Username" class="form-control" placeholder="Kullanıcı adınızı girin" required />
                    </div>
                    <span asp-validation-for="Username" class="text-danger"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="Password" class="form-label">Şifre</label>
                    <div class="input-group">
                        <span class="input-group-text">
                            <i class="fas fa-lock"></i>
                        </span>
                        <input asp-for="Password" class="form-control" placeholder="Şifrenizi girin" required />
                    </div>
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>

                <div class="d-grid">
                    <button type="submit" class="btn btn-primary btn-lg">
                        <i class="fas fa-sign-in-alt me-2"></i>
                        Giriş Yap
                    </button>
                </div>

                @if (ViewData["ReturnUrl"] != null)
                {
                    <input type="hidden" name="returnUrl" value="@ViewData["ReturnUrl"]" />
                }
            </form>

            <div class="text-center mt-4">
                <div class="mt-3">
                    <form asp-controller="Auth" asp-action="CreateTestUsers" method="post" class="mb-3">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-users me-2"></i>
                            Test Kullanıcılarını Oluştur
                        </button>
                    </form>
                </div>
                
                <div class="mt-3">
                    <h6 class="mb-3 text-center">🚀 Hızlı Test Giriş</h6>
                    
                    <!-- İlk satır: Admin ve Manager -->
                    <div class="row g-2 mb-2">
                        <div class="col-6">
                            <form asp-controller="Auth" asp-action="TestAuth" method="post" class="d-inline">
                                <input type="hidden" name="username" value="admin" />
                                <input type="hidden" name="password" value="admin" />
                                <button type="submit" class="btn btn-sm btn-danger w-100 fw-bold">
                                    <i class="fas fa-crown me-1"></i>
                                    Admin
                                </button>
                            </form>
                        </div>
                        <div class="col-6">
                            <form asp-controller="Auth" asp-action="TestAuth" method="post" class="d-inline">
                                <input type="hidden" name="username" value="manager" />
                                <input type="hidden" name="password" value="manager" />
                                <button type="submit" class="btn btn-sm btn-primary w-100 fw-bold">
                                    <i class="fas fa-user-tie me-1"></i>
                                    Manager
                                </button>
                            </form>
                        </div>
                    </div>
                    
                    <!-- İkinci satır: Sales ve Purchase -->
                    <div class="row g-2 mb-2">
                        <div class="col-6">
                            <form asp-controller="Auth" asp-action="TestAuth" method="post" class="d-inline">
                                <input type="hidden" name="username" value="sales" />
                                <input type="hidden" name="password" value="sales" />
                                <button type="submit" class="btn btn-sm btn-success w-100 fw-bold">
                                    <i class="fas fa-shopping-cart me-1"></i>
                                    Sales
                                </button>
                            </form>
                        </div>
                        <div class="col-6">
                            <form asp-controller="Auth" asp-action="TestAuth" method="post" class="d-inline">
                                <input type="hidden" name="username" value="purchase" />
                                <input type="hidden" name="password" value="purchase" />
                                <button type="submit" class="btn btn-sm btn-info w-100 fw-bold">
                                    <i class="fas fa-shopping-bag me-1"></i>
                                    Purchase
                                </button>
                            </form>
                        </div>
                    </div>
                    
                    <!-- Üçüncü satır: Finance ve Warehouse -->
                    <div class="row g-2 mb-2">
                        <div class="col-6">
                            <form asp-controller="Auth" asp-action="TestAuth" method="post" class="d-inline">
                                <input type="hidden" name="username" value="finance" />
                                <input type="hidden" name="password" value="finance" />
                                <button type="submit" class="btn btn-sm btn-warning w-100 fw-bold">
                                    <i class="fas fa-calculator me-1"></i>
                                    Finance
                                </button>
                            </form>
                        </div>
                        <div class="col-6">
                            <form asp-controller="Auth" asp-action="TestAuth" method="post" class="d-inline">
                                <input type="hidden" name="username" value="warehouse" />
                                <input type="hidden" name="password" value="warehouse" />
                                <button type="submit" class="btn btn-sm btn-secondary w-100 fw-bold">
                                    <i class="fas fa-warehouse me-1"></i>
                                    Warehouse
                                </button>
                            </form>
                        </div>
                    </div>
                    
                    <!-- Dördüncü satır: Employee (tam genişlik) -->
                    <div class="row g-2">
                        <div class="col-12">
                            <form asp-controller="Auth" asp-action="TestAuth" method="post" class="d-inline">
                                <input type="hidden" name="username" value="employee" />
                                <input type="hidden" name="password" value="employee" />
                                <button type="submit" class="btn btn-sm btn-outline-dark w-100 fw-bold">
                                    <i class="fas fa-user me-1"></i>
                                    Employee
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
                
                <div class="mt-3">
                    <small class="text-muted d-block text-center">
                        <i class="fas fa-info-circle me-1"></i>
                        <strong>Test Kullanıcıları:</strong><br>
                        admin, manager, sales, purchase, finance, warehouse, employee<br>
                        <span class="text-primary">Tüm şifreler kullanıcı adı ile aynı</span>
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
} 